export enum ViewType {
  TABLE = "table",
  GRID = "grid",
}

export enum BeanAttribute {
  GLUTEN_FREE = "GlutenFree",
  SUGAR_FREE = "SugarFree",
  SEASONAL = "Seasonal",
  KOSHER = "Kosher",
}

export enum SortType {
  ASC = "asc",
  DESC = "desc",
  GROUP = "group",
  COLOR = "color",
}